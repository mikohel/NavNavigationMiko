<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.navnavigationmiko.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_homeFragment_to_insideHome"
            app:destination="@id/insideHome" />
    </fragment>
    <fragment
        android:id="@+id/insideHome"
        android:name="com.example.navnavigationmiko.InsideHome"
        android:label="fragment_inside_home"
        tools:layout="@layout/fragment_inside_home" >
        <argument
            android:name="nombre"
            app:argType="string" />
        <argument
            android:name="appaterno"
            app:argType="string" />
        <argument
            android:name="apmaterno"
            app:argType="string" />
        <argument
            android:name="usuario"
            app:argType="string" />
        <argument
            android:name="email"
            app:argType="string" />
    </fragment>
    <fragment
        android:id="@+id/profileFragment"
        android:name="com.example.navnavigationmiko.ProfileFragment"
        android:label="fragment_profile"
        tools:layout="@layout/fragment_profile" />
    <fragment
        android:id="@+id/settingsFragment"
        android:name="com.example.navnavigationmiko.SettingsFragment"
        android:label="fragment_settings"
        tools:layout="@layout/fragment_settings" />
</navigation>